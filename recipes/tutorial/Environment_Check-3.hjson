{
  settings:
  {
    name: Environment Check
    image: environment.jpg
    help:
      '''
      This recipe prints the bash environment used to run the script.
      # Help

      This recipe uses the `printenv` command to display the current environment.

      It prints the information into a file for later processing but also displays it on the standard output.
      '''
    id: 3
    uid: environment
    project_uid: tutorial
    url: http://localhost:8000
  }
}